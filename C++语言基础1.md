
1. cout << "hello" <<", "<<"world!"<< endl;双引号而非单引号
2. cin >> "b";
3. if (a >5){
		cout << "a > 5" <<endl;
    } else if (a = 5){
		 cout << "a =5" << endl;
	} else{
		 cout << "a < 5" <<endl;
	}  
4.for：循环

	for( int i=0; i < 5; i++){
		cout << "a" << endl;
	}
5.while循环
  
	while(nums >0){
	cout << nums << " "
	num /= 2;
	}
6..    初始化数组

	2. vector<int> nums; //初始化一个int型空数组nums
	3. vector<int> nums(n); //初始化值为0的大小为n数组nums
	4. vector<int> nums{1, 3, 5}; //初始化值为1， 3， 5.**==应该为{}==**
	5. vector<int> nums(n, 2);// 初始化值全为2的大小为n数组
	6. vector<vector<int>> dp; //初始化2维数组dp
	7. vector<vector<int>> dp(m, vector<bool>(n, true));
7.vector的常用操作

	1.cout << nums.empty() << endl;//数组nums是否为空 0非空
	2.cout << nums.size() << endl;//数组大小
	8. nums.push_back(20); //在数组nums尾部推入一个20
	9. cout << nums.back() << endl;//数组最后的元素进行引用
	10. nums.pop_back();//弹出nums尾部的元素。
	11. nums[0] = 11;//方括号进行数值的修改 cout << nums[0] << endl;//进行数值的取值
	12. nums.insert(nums.begin()+3,12);//插入到索引3，数值为12
	13. nums.erase(nums.begin()+2);//删除索引2
	14. swap(nums[2], nums.[4]); //交换2，4
	15. for(int i=0; i < nums.size(); i++){ //遍历整个数组为nums.size()
			cout << nums[i] << " ";  //此处不需要endl，在一行进行输出，空格就行。
		}
		cout << endl;