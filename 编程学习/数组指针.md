## 26.去除重复
首先判断是不是空数组，是的话直接没有索引
接着快慢指针的索引进行对比，相等则fast直接继续
否则slow++；并对slow的索引进行赋值。
slow ++；先开辟新空间
nums(slow)=nums(fast);再填值

## 83.删除排序链表中的重复元素
fast!=nullptr就可以啦
slow->next = fast;有了next，就可以移动指针了。
## 27.移除元素
nums(slow)=nums(fast);先填值
slow ++；再开辟新空间

## 283.移动零
int p = removeElement(nums, 0);
// 将 p 之后的所有元素赋值为 0 
for (; p < nums.size(); p++) { 
	nums[p] = 0;
	 }
 直接从p之后进行赋值。
``int removeelement(vector<int>& nums, val){``
应该为==int val==

## 二分
left + (right - left)/2;不会出现left+right大数相加导致的溢出
