爬台阶最低花费

1. 确定dpi以及下标的意义
	到第i台阶最少体力`dp[i]`
2. 推导递归公式
	`dp[i] = min(dp[i-1]+cost[i-1], dp[i-2]+cost[i-2])`
3. 进行初始化
	`dp[0] = 0  dp[1] = 0`
4. 遍历顺序
	i=2开始(因为下标为3)，也就是第三。
5. 举例推导
版本一

关注为什么是cost.size()+1

cost.size()是楼顶

版本二

0是前前个状态，1为前个状态

dpi  dp0 = dp1 dp1 = dpi

## 62.不同路径

二叉树：直接进行递归

1. 确定`dp[i][j]`以及下标的意义
	`dp[i][j]`:从0，0到i,j
2. 推导递归公式
	`dp[i][j] = dp[i-1][j] + dp[i][j-1]`
3. 进行初始化
	`for (int i(j) = 0; i(j) < m; i++) dp[i][0] = 1  dp[0][j] = 1`
4. 遍历顺序
	`dp[i][j]`
5. 举例推导
	

