## 235.二叉搜索树的公共祖先
### 知识点：二叉搜索树特性：左值小于中值小于右值的特点
二叉搜索树（BST）有什么特殊的性质？这个性质如何帮助我们不用遍历整棵树就能找到目标？

### 对答案：BST 的核心性质是：**左子树所有节点值 < 根节点值 < 右子树所有节点值**。
我们要找 `p` 和 `q` 的最近公共祖先（LCA），只需要从根节点 `root` 开始遍历：

- **情况 A**：如果 `p` 和 `q` 的值都 **大于** 当前节点值 -> 说明它是往 **右** 走的，LCA 肯定在右子树。
    
- **情况 B**：如果 `p` 和 `q` 的值都 **小于** 当前节点值 -> 说明它是往 **左** 走的，LCA 肯定在左子树。
    
- **情况 C**：如果不满足上述两种情况（即一个大一个小，或者其中一个就是当前节点） -> **当前节点就是“分岔点”，也就是最近公共祖先**。
    

**结论**：只要找到第一个让 `p` 和 `q` 分别位于两侧（或其中之一）的节点，就是它了。不需要回溯，不需要像普通二叉树那样用后序遍历。

上级：

- **写**：**二叉树的遍历 / 树的搜索**
    
- **想**：二叉树主要有哪几种遍历方式？（前序、中序、后序、层序）。这道题其实利用了 BST 的特性，本质上是一种类似于**二分查找**的搜索路径，每次可以排除掉一半的分支。

平级：

- **写**：**普通二叉树的最近公共祖先 (LeetCode 236)**
    
- **想**：如果题目去掉“搜索”两个字，变成普通二叉树，还能用上面的 `if-else` 大小比较吗？
    
    - _答案_：不能。普通二叉树没有大小规律，必须使用**后序遍历**（左右根），从底向上回溯，看左右子树是否包含 `p` 和 `q`。这是面试中这道题最容易被追问的“兄弟问题”。
        
- **写**：**验证二叉搜索树 (LeetCode 98)**
    
- **想**：如何验证一棵树是 BST？同样是利用中序遍历有序，或者递归限定上下界。

往下：

- **写**：**BST 的定义 (Binary Search Tree)**
    
- **想**：
    
    1. 节点的左子树只包含**小于**当前节点的数。
        
    2. 节点的右子树只包含**大于**当前节点的数。
        
    3. 所有左、右子树自身必须也是二叉搜索树。
        
- **写**：**指针操作**
    
- **想**：C++ 中 `root = root->left` 是什么意思？（指针指向了左孩子内存地址，完成了“移动”）。
### 递归法demo
```cpp
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        if (root->val > p->val && root->val > q->val) {
            return lowestCommonAncestor(root->left, p, q);
        } else if (root->val < p->val && root->val < q->val) {
            return lowestCommonAncestor(root->right, p, q);
        } else return root;
    }
};
```
### 迭代法demo
```cpp
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        while(root) {
            if (root->val > p->val && root->val > q->val) {
                root = root->left;
            } else if (root->val < p->val && root->val < q->val) {
                root = root->right;
            } else return root;
        }
        return NULL;
    }
};
```

> **你的笔记**：普通二叉树（LeetCode 236）没有规律，必须后序遍历，从底向上回溯。

- **👨‍🏫 批注（面试高频点）**：
    
    - **路径方向**：这是最本质的区别。
        
        - **BST (235题)**：是**自上而下 (Top-down)** 的。像走迷宫，站在路口（根节点）就知道往左还是往右，**不需要回头**。
            
        - **普通树 (236题)**：是**自下而上 (Bottom-up)** 的。像找人，必须先下到最底下，找到了再一层层汇报上来（回溯）。
            
    - **代码特征**：
        
        - BST 的代码通常没有 `return left + right` 这种汇总逻辑，而是 `return left` **或者** `return right`（单边逻辑）。
## 701.二叉搜索树中的插入操作
### 第一步：题目本身（了解考查知识点）

**请先停下来，给自己 1-5 分钟思考以下问题：**

> 题目说“可能存在多种有效的插入方式”，但为了简单（且不出错），我们通常选择把新节点放在哪里？
> 
> 1. 是硬塞到树的中间（那样可能需要把原来的节点挪位置）？
>     
> 2. 还是顺着树往下找，直到找到一个 **空位 (Leaf)** 挂上去？
>     
> 
> **提示**：回顾一下刚才做的 **LCA（最近公共祖先）** 或者是 **700. 二叉搜索树中的搜索**。如果你要找这个 `val`，你会走到哪里？如果那个位置是空的，是不是正好放进去？

### 第二步：对答案（逻辑与代码）

#### 1. 核心逻辑

**最简单的策略：永远插在叶子节点（空节点）位置。**

根本不需要重组树的结构（不需要旋转）。你只需要像“搜索”一个值一样去遍历树：

- 如果 `val` 比当前节点大 -> 往右走。
    
- 如果 `val` 比当前节点小 -> 往左走。
    
- **直到走到 `NULL`** -> 说明这里就是它的归宿！在这个位置 `new` 一个新节点，并把它和父节点连起来。

### 第三步：回顾知识点（构建体系）

拿出纸笔，按照层级回顾：

#### 1. 往上走（大一级知识点）

- **写：BST 的修改操作 (CRUD)**
    
- **想**：我们之前只做了“查”（Search/LCA）。现在做的是“增”（Insert）。
    
    - “增”其实就是“查”的变体：**查不到的地方，就是该增的地方。**
        

#### 2. 平级走（同级知识点）

- **写：二叉搜索树的删除 (LeetCode 450)**
    
- **想**：如果让你删除一个节点，会比插入简单还是难？
    
    - _答案_：**难得多**。
        
    - 插入只是挂在末尾（叶子），不影响别人。
        
    - 删除如果删的是中间节点，它的左右孩子怎么办？谁来接替它的位置？（这个问题留个悬念，是 BST 中最复杂的逻辑）。
        

#### 3. 往下走（子知识点）

- **写：内存分配 (`new`)**
    
- **想**：C++ 中 `new TreeNode(val)` 做了什么？（在堆区开辟内存）。
    
- **写：递归函数的返回值接驳**
    
- **想**：为什么要写 `root->left = insert(...)` 而不是直接调用 `insert(...)`？
    
    - 因为如果下一层创建了新节点，它必须把这个新节点的**地址**传回来，上一层必须用指针**接住**，链条才能连上。如果不接，新节点就悬空了（内存泄漏且没连上树）。
### 迭代法
### 第一步：题目本身（逻辑推演）

**思考核心痛点：** 在递归法中，我们到了空节点（`NULL`）直接 `return new Node`，上一层的递归函数会帮我们把这个新节点接住（`root->left = ...`）。 但在迭代法（`while` 循环）中，当你走到 `NULL` 时，你手里只有一个空指针，你已经**丢失了**它的父节点是谁。

> _比喻：你走迷宫走到了死胡同（NULL），想在死胡同口插个旗子，但你发现你连这堵墙是属于哪个路口的都忘了。_

**解决方案：** 我们需要一个**“双指针”**策略。

- `cur`：负责探路，一直走到 `NULL` 为止。
    
- `parent`：负责**记录**，永远跟在 `cur` 后面一步。当 `cur` 掉进空坑里时，`parent` 正好站在悬崖边，这时候就可以通过 `parent` 把新节点拉上来。

第二步：对答案（标准代码）
```cpp
class Solution {
public:
    TreeNode* insertIntoBST(TreeNode* root, int val) {
        if (root == NULL) {
            TreeNode* node = new TreeNode(val);
            return node;
        }
        TreeNode* cur = root;
        TreeNode* parent = root; // 这个很重要，需要记录上一个节点，否则无法赋值新节点
        while (cur != NULL) {
            parent = cur;
            if (cur->val > val) cur = cur->left;
            else cur = cur->right;
        }
        TreeNode* node = new TreeNode(val);
        if (val < parent->val) parent->left = node;// 此时是用parent节点的进行赋值
        else parent->right = node;
        return root;
    }
};
```

**代码细节自我拷问：**

1. **为什么需要 `parent = cur`？**
    
    - 因为 `cur` 最后变成了 `NULL`，你无法对 `NULL` 操作 `NULL->left`。必须操作 `parent->left`。
        
2. **最后的 `if-else` 是干嘛的？**
    
    - 我们虽然找到了 `parent`，但不知道新节点该挂在 `parent` 的左腿还是右腿上，所以需要再比一次大小 (`val < parent->val`)。

### 第三步：回顾知识点（构建体系）

#### 1. 往上走（大一级知识点）

- **写：双指针法 (Two Pointers)**
    
- **想**：这其实和**链表 (Linked List)** 的操作非常像。
    
    - 想一想：在单链表中删除或插入一个节点，是不是也需要一个 `prev` 指针指向前一个节点？因为单向结构没法回头。树就是一种复杂的单向链表。
        

#### 2. 平级走（同级知识点）

- **写：递归 vs 迭代**
    
- **想**：
    
    - 递归利用**系统栈**（Call Stack）隐式地记录了路径，所以 `return` 时能回到父节点。
        
    - 迭代利用**显式变量**（`parent` 指针）来记录路径。
        
    - 对于这道题，**迭代法效率更高**，因为它没有函数调用的开销，空间复杂度恒定为 $O(1)$。
        

#### 3. 往下走（子知识点）

- **写：边界条件处理**
    
- **想**：这就好比做数学题的“定义域”。
    
    - 如果 `root` 一开始就是空的（`NULL`），`cur` 和 `parent` 进不去 `while` 循环，下面的逻辑会崩。所以必须开头就写 `if (root == NULL)`。**写算法题，第一行永远先想边界。**

## 450.删除二叉搜索树中的节点
### 第二步：对答案（逻辑深度解析）

**判决：完全正确。** 你不仅处理了逻辑，还手动处理了内存释放（`delete`），这在 C++ 面试中是巨大的加分项（Java/Python 选手通常会忽略这一点）。

我们重点拆解最复杂的 **Case 5 (左右都有孩子)**，看看你的代码到底做了什么：

```cpp
// 你的代码逻辑：
TreeNode* cur = root->right; // 1. 先去右边
while(cur->left != nullptr) {
    cur = cur->left;         // 2. 一直向左走，找到右子树里“最小”的那个节点
}
cur->left = root->left;      // 3. 【关键】把要删除节点的“整个左子树”，挂在这个最小节点的左下角
root = root->right;          // 4. 右孩子上位，成为新的根
```

**为什么要这么做？（可视化理解）**

- **目标**：删除 `root`，但 `root` 的 `left` 子树这就成了“孤儿”。
    
- **安置孤儿**：根据 BST 性质，`root` 的 `left` 子树里所有节点，都**小于** `root` 的 `right` 子树里所有节点。
    
- **寻找养父**：`root` 的 `right` 子树里，**谁**最小？当然是**最左边**的那个节点（代码里的 `cur`）。因为 `cur` 是右边最小的，但它依然比左子树所有人都大。
    
- **结论**：所以把 `root->left` 接到 `cur->left` 上，完全符合 BST 的大小规则。

### 第三步：回顾知识点（构建体系）

#### 1. 往上走（大一级知识点）

- **写：BST 的性质应用**
    
- **想**：这道题是 BST 性质的集大成者。
    
    - Case 1-4 利用了**树的结构**（直接断开或替补）。
        
    - Case 5 利用了**数值的大小有序性**（左子树 < 右子树最小节点）。
        

#### 2. 平级走（同级知识点）

- **写：Case 5 的另一种解法（“偷梁换柱”法）**
    
- **想**：你的写法是**修改指针**（改变了树的形状，树可能会变高）。还有一种不改变树形状太多的方法：
    
    1. 找到右子树的最小节点（和你的 `cur` 一样）。
        
    2. 把 `cur->val` 赋值给 `root->val`（**只替换值，不删节点**）。
        
    3. 递归去右子树删除那个 `cur` 节点。
        
    
    - _区别_：你的方法是物理删除，操作指针（O(1)），但树可能变不平衡；“偷梁换柱”法是递归删除值，树的结构保持得更好。**面试时写你这种就够了，但要能说出另一种。**
        

#### 3. 往下走（子知识点）

- **写：C++ 内存管理 (`delete`)**
    
- **想**：
    
    - 在算法题中，通常为了省事不写 `delete`（因为判题系统最后会统一回收进程）。
        
    - 但在**工程/面试**中，你写的 `delete tmp` 至关重要。C++ 中 `new` 出来的对象都在**堆区 (Heap)**，不删就是**内存泄漏 (Memory Leak)**。你这一点做得非常好。

### 1. 核心矛盾：两个孩子怎么办？

**场景**：你要删除节点 `root`（比如值为 5），它有两个孩子：

- 左孩子 `left`（代表所有比 5 小的一大家子，比如 2, 1, 3）。
    
- 右孩子 `right`（代表所有比 5 大的一大家子，比如 7, 6, 8）。
    

**难题**：如果你把 5 删了，这两个孩子（左和右）就断开了。你必须把它们**重新连成一棵树**，而且必须还是一棵 **BST**（依然满足“左 < 根 < 右”）。

---

### 2. 你的策略：托付给右边的“最小弟”

你的代码逻辑是这样的：

1. **选新主**：既然 5 要走了，通常选**右孩子**（7）上位，接替 5 的位置。
    
2. **安置旧部**：那 5 原来的左孩子（2 那一家子）怎么办？不能扔啊。
    
3. **寻找接盘侠**：根据 BST 性质，左孩子（2）比右边所有人都小。所以，我们要把左孩子挂在**右子树中“数值最小”**的那个节点的下面。
    

**为什么是右子树中“最小”的节点？**

- 因为那个最小的节点（位于右子树的最左下角），它肯定没有左孩子了（如果有，它就不是最小的了）。
    
- 它正好有一个**空出来的左腿**，可以用来挂 5 原来的左子树。
    

---

### 3. 逐行代码图解

假设树是这样的：

Plaintext

```cpp
      5 (root, 要删)
     / \
    2   7
       / \
      6   8
```

#### 第一步：`TreeNode* cur = root->right;`

我们先把目光投向右子树（根节点是 7）。

#### 第二步：寻找“接盘侠” (While 循环)

```cpp
while(cur->left != nullptr) {
    cur = cur->left;
}
```

- 我们从 7 开始，一直往左走。
    
- 找到了 **6**。
    
- 6 的左边是空的（`nullptr`）。
    
- **结论**：6 就是右子树里最小的节点，也是我们选定的“接盘侠”。
    

#### 第三步：嫁接左子树 (关键操作)

```cpp
cur->left = root->left;
```

- 把 `root` 的左子树（也就是以 2 为根的那一坨），整个拎过来。
    
- 接在 6 的左孩子位置上。
    

此时树变成了这样（逻辑结构）：

```cpp
      5 (待删)
       \
        7 (新根)
       / \
      6   8
     /
    2 (原来的左子树被接到了这里)
```

#### 第四步：旧王退位，新王登基

```cpp
root = root->right;
delete tmp;
return root;
```

- 原来的 `root` (5) 被删除。
    
- 返回 `root->right` (7) 作为整棵树新的根节点。
    

最终形态：

```cpp
        7
       / \
      6   8
     /
    2
```

**检查一下**：这依然是一棵 BST 吗？

- 6 是 7 的左孩子（6 < 7），对。
    
- 2 是 6 的左孩子（2 < 6），对。
    
- 原来的左子树所有值 < 原 root < 原右子树所有值。所以逻辑完全成立。