## 704.二分查找
int middle==定义在循环里==，他是在更新的

不能定义在循环外部。
## 27、移除元素（原地）
快慢指针法

快指针遍历，慢指针进行留存val。[快慢指针套路](../数组指针.md#^973250)
## 977.有序数组的平方
暴力解法
`a[i] *= a[i]`
`sort(a.begin(), a.end())`

相向双指针
```cpp
for (int i = 0, j = A.size() - 1; i <= j;)
i++;//left
j--;//right
```