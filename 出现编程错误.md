1. main函数不进行定义
2. cin >> 命令不需要进行手动空格“ ”以及回车endl。
3. Solution solution；第一个是类；第二个则是实例。solution.twoSum（参数）；twoSum(技能)，参数需要进行输入定义，cin。
4. vector<int> result = solution.twoSum(nums, target);vector<int> result接受成品。
5. return 0;表示main ok状态。return通常有两个意义，一个是结束函数（终止），一个传递值（交货）。
6. set.count(num);:检查登记簿有无这个数字。
7. unordered_set<==int==> uset;我把int忘了
8. count[num]这个是键的值；count[num]++;对值进行加1也就是计数；time++；tickets[]++。
9. char要用'',不能用“”
10. 买票时候的q.front()在q.pop()后就会变化；front = q.front()则被留下。
11. continue的后面不会再执行，直接进入下一轮
12. 时间复杂度不止看for；例如vector<int> nums(n);空间是n、时间也是n。
13. 4个字节储存一个元素的值；arr[0]:首地址、arr[1]往后四个字节。
14. 为什么要初始化；int arr[n]：会开辟空间，但你不知道这片空间是不是一手的还是用过的
15. 数组的扩容会涉及新数组的开辟，因为内存地址不能随意的增减。插入与满数据情况n