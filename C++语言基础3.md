1. 哈希集合unordered_set:储存不重复元素；可对==元素进行去重==
2. 初始化方法
	1. unordered_set<int> uset;//初始化一个空的哈希集合uset
	2. unordered_set<int> uset{1, 2, 3, 4};//初始化具备一些元素的哈希集合
3. 哈希集合常用的方法
	1. cout << uset.empty() << endl;
	2. cout << uset.size() <<endl;
	3. if(uset.contain(3)){
		cout << "Element 3 found." << endl;
	  } else{
		cout << "Element 3 not found." << endl;
	  }//查找指定元素是否存在
	4. uset.insert(5);插入一个新元素
	5. uset.erase(2);删除元素2
	6. for (const auto &element : uset){
		cout << element << endl; 
	   }
  4. 