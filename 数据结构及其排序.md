1. 时间与空间复杂度
	1. 时间复杂度：耗费时间，按最大来
	2. 空间复杂度：占用多少内存；arr[n]
2. 链表
	1. 单向链表head指向整个链表，找到链表
		1. head不能变，不然钥匙丢了，就找不回来啦
	2. next负责链接
	3. ListNode* cur = head;：替身在左，只复制右边的值。
	4. cur = cur->next;替换了指针指向的位置，而非节点；改变了指向
	5. cur->val = 5；改变了苹果
3. for循环很像是左闭右闭（开）的那种数列