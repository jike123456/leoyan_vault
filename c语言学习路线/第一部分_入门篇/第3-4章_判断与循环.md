
#### 第3-4章：判断与循环 (逻辑核心)

- **内容**：`if-else`，`switch-case`，`while`，`do-while`，`for`。
    
- **重点**：
    
    - **循环的选择**：什么时候用 `for`（已知次数），什么时候用 `while`（未知次数）。
        
    - **边界条件**：循环是从 0 开始还是从 1 开始？这是新手最容易写出 Bug 的地方（Off-by-one error）。
        
    - **Switch**：在嵌入式状态机（State Machine）编程中非常常用。