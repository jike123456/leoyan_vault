#### 第5章：数据类型 (嵌入式必修)

- **内容**：整数、浮点数、字符、布尔值。
    
- **重点 (★嵌入式关键)**：
    
    - **sizeof**：理解不同数据类型在内存中占几个字节（例如 `int` 在 8位单片机可能是 2字节，在 32位单片机是 4字节）。
        
    - **补码**：计算机底层是如何存储负数的。
        
    - **范围**：数据溢出（Overflow）会发生什么？