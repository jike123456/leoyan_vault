#### 第12章：链表与位运算 (Bitwise)

- **内容**：动态内存分配 (`malloc/free`)，链表，位操作 (`& | ^ ~ << >>`)。
    
- **重点 (★★★)**：
    
    - **位运算**：翁恺老师这部分讲得可能比较快，但对于控制硬件（如点亮一个 LED 灯、读取寄存器某一位）是**绝对核心**，你需要额外多练习。
        
    - **链表**：理解数据结构的基础，操作系统内核（如 RTOS）里全是链表。