## ln命令创建软链接
在系统中创建软链接，可以将文件或者文件夹链接到其他的位置

就类似与windows中的快捷方式

语法：ln -s 参数一  参数二
- 选项s：创建软链接
- 参数一：文件/文件夹
- 参数二：要去的目的地

示例：
- ln -s  /etc/yum.conf ~/yum.conf
- ln -s /etc/yum ~/yum[[Linus常见命令#^8544eb|home目录]]
- cd直接进入home目录