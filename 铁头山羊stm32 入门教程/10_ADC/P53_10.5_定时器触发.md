# 10.5 [ADC]定时器触发

## 主要内容
使用定时器更新事件触发 ADC 转换，实现固定频率采样 (如音频采集)。

### 配置
*   **ADC Trigger**: `ADC_ExternalTrigConv_T2_CC2` (举例)。
*   **TIM2**: 配置为 PWM 或 Output Compare 模式，产生信号。
*   这样无需软件循环调用 `StartConv`，硬件自动触发。
