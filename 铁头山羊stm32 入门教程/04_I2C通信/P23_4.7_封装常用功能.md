# 4.7 [I2C]封装常用功能

## 主要内容
封装 I2C 读写寄存器的通用函数。

### 通用函数
*   `I2C_WriteReg(uint8_t DevAddr, uint8_t RegAddr, uint8_t Data)`
*   `I2C_ReadReg(uint8_t DevAddr, uint8_t RegAddr)`

这些函数内部调用 Start, SendByte(DevAddr), SendByte(RegAddr), SendByte(Data)/ReceiveByte, Stop 等底层操作。
