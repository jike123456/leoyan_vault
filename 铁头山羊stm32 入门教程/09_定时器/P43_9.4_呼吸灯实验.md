# 9.4 [定时器]呼吸灯实验

## 主要内容
代码演示 PWM 呼吸灯。

### 引脚重映射 (Remap)
*   TIM2 的 CH1 默认在 PA0。
*   如果 PA0 被占用，可以使用 Remap 功能映射到 PA15。
    *   `RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO, ENABLE);`
    *   `GPIO_PinRemapConfig(GPIO_PartialRemap1_TIM2, ENABLE);`
